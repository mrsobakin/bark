[package]
name = "bark-jni"
version = "0.1.0"
edition = "2021"
description = "JNI bindings for bark-core"

[lib]
crate-type = ["cdylib"]

[dependencies]
bark-core = { path = "../bark-core" }
jni = "0.21"
serde_json = "1"