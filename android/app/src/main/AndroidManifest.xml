<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bark"
        android:usesCleartextTraffic="true">

        <!-- The IME service – this is the keyboard itself -->
        <service
            android:name=".BarkKeyboardService"
            android:exported="true"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- Activity for configuring the endpoint URL -->
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/settings_title"
            android:theme="@style/Theme.Bark.Settings" />

        <!-- Transparent activity that shows the mic permission dialog on < 33 -->
        <activity
            android:name=".PermissionBridgeActivity"
            android:exported="false"
            android:theme="@style/Theme.Bark.Translucent"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="true" />

    </application>

</manifest>
