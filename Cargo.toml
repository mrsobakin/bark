[workspace]
members = ["core", "jni", "daemon"]
resolver = "2"

[profile.release]
strip = true
lto = true

[workspace.package]
version = "0.1.0"
edition = "2021"

[workspace.dependencies]
bark-core = { path = "core" }
