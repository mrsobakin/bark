[package]
name = "bark-demo"
version = "0.1.0"
edition = "2021"
description = "Simple CLI demo: record from mic then transcribe with bark-core"

[dependencies]
bark-core = { path = "../bark-core" }
cpal = "0.15"
ctrlc = "3.4"
anyhow = "1"
